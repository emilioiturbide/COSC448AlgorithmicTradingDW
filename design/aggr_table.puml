@startuml

entity "agg_15min_raw" {
    * symbol : VARCHAR(10) <<PK>>
    * interval_start : TIMESTAMP <<PK>>
--
    * symbol_type : VARCHAR(20)
    * open : NUMERIC(18,6)
    * high : NUMERIC(18,6)
    * low : NUMERIC(18,6)
    * close : NUMERIC(18,6)
    * volume : BIGINT
    * trade_count : INT
}
@enduml